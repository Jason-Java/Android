package com.example.javademo.domain;

public class SqlHelp {


  /*  public static String parameterSubstitution(String sql, int value) {
        return "";
    }

    public static String parameterSubstitution(String sql, Object... value) {
        int count = 0;
        for (int i = 0; i < sql.length(); i++) {
            if (sql.charAt(i) == '?') {
                count++;
            }
        }
        if (count != value.length) {
            throw new RuntimeException("占位符个数和参数个数不匹配");
        }
        int index = -1;
        count=0;
        while ((index = sql.indexOf('?')) != -1) {

        }
        return "";
    }*/

}
