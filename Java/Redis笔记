说明：<> 表示必须值   [] 表示可选值

Redis一共有16个库0-15
切换库：
	select <index> 如 select 8

keys *：查看当前库中所有的key

exists key：判断某个Key是否存在

type key:查看key是什么类型

del key：删除指定的Key数据

unlink key：根据Value选择非阻塞删除

expire key n：设置key n秒后过期

ttl key：查看key还有多少秒过期 -1表示永不过期，-2表示已经过期

dbseize：查看当前数据库中有多少key

flushdb：清空当前数据库

flushall：清空所有的数据库

set <key> <value> [ex seconds|px milliseconds]
	说明：将字符串添加到redis数据库中
		ex表示单位秒
		px表示毫秒 与ex互斥
	例如：set k1 v100 ex 10
setex <key> <过期时间> <value>
	说明：设置键值对的同时设置过期时间
getset <key> <newValue>
	说明：以新值换旧值 当前取出的值还是旧值 
append <key> <value>
	说明：追加字符
strlen <key>
	说明：查看key的长度
setnx <key> <value>
	说明：只有载key 不存在时候才设置value 存在时不设置
incr <key>
	说明：将key中存储的数字值加1，只能针对数字值，如果为空，新增值为1
decr <key> 
	说明：将key中存储的数字值减1，只针对数字值，如果为空，新增值为-1

mset <key> <value> <key1> <value2>......
	说明：同时设置多个键值对
mget <key> <key1> <key2>.......
	说明：同时获取多个key值
msetnx 同时设置多个键值对 但是msetnx具有原子性，其中一个失败其他设置都失败

getrange <key> <start> <end>
	说明：获取指定key中一个范围子字符串 包括start和end位置
setrange <key> <offset> <value>
	说明：从offset位置重新设置值 覆盖原有位置的值 




list 操作

rpush/lpush  <key> <value> <value2> <value3>..............
	说明：从list左边或右边开始压入数据
rpop/lpop <key>
	说明：从list左边或右边弹出一个值 值在键在，值亡键亡
rpoplpush <key1><ke2> 
	说明：从key1的右边吐出一个值插入到key2的左边
lrange <key> <start><end> 
	说明：按照索引从左到右获取list中的值 0 -1 表示获取所有的值
lindex <key> <index>
	说明：按照索引下表获取元素（从左边开始）
<<<<<<< HEAD
linsert <key> <before|after> <value> <newValue>
	说明:在value之前或之后插入一个新值
lrem <key> <count> <value>
	说明: 删除 count 个 value值
lset <key> <index> <value>
	说明:将key下表为index的值重新设置为value
	
	
	
	
	
	set操作
sadd <key> <value1> <value2> <value3>......
	说明:给 key添加1个或多个值
smembers <key>
	说明:去除key中的全部元素
sismember <key> <value>
	说明:key中是否有value   0没有 1有
scard <key> 
	说明:查看key的元素个数
	
spop <key> [count]
	说明:从key中随机吐出count个值 count 可以不写默认为1
srandmember <key> [count]
	说明:从key中随机取出count个值但不删除   count 可以不写默认为1
smove <sourceKey> <destinationKey> <value>
	说明:将一个值从一个集合移动到另外一个集合
=======

	Zset操作
zadd <key> <score> <value> [<score> <value>......]
	说明：向Zset集合中添加一个有序的 score 是在集合中的排序 从小到大进行排序
zreange <key> <start> <end> [withscores]
	说明：在集合中获取下标在指定范围内的值，包含两端的值 zrange key 0 -1 表示获取全部内容withscores 表示评分也一起输出

zrangebyscore <key> <min> <max> [withscores] [limit offset count]
	说明：取评分范围内的值  offset 表示从第几行开始取 count 表示取出来的数量

zrevreange <key> <start> <end> [withscores]
	说明：倒序输出
zrevreangescore <key> <max> <min>
	说明：倒序输出

zrem <key> <member> 
	说明：删除指定元素
zincrby  <key> <increment> <member>
	说明：给指定成员增加指定分数


发布订阅机制

subscribe <channelName>
	说明：客户端订阅channelName这个通道的消息
publish <channelName> <message>
	说明：客户端向channelName这个通道发送消息



bitmap操作

setbit <key> <offset> <value>
	说明： offset 偏移量  value取范围0，1
	       向偏移量位置标记为1或0
getbit <key> <offset>
	说明：获取偏移量位置的值
bitcount <key> [start end] 
	说明：获取key中1的个数
bitop and/or/not/xor
>>>>>>> 9b2a7dbb9e8246a8237e041b9bf89e6c56098c99





<<<<<<< HEAD
	hash操作
hset <key> <filed> <value> [<filed> <value>......]
	说明:设置key 的filed的值为value file和value可以有多对
hget <key> <filed>
	说明:从可以中取出filed的值
hexists <key> <filed>
	说明 key中是否存在filed字段
hkeys <key>
	说明:取出key中的所有filed
hvals <key>
	说明:取出key中的所有value
hsetnx <key><field><value>


   Zset操作
zadd <key> score

=======
>>>>>>> 9b2a7dbb9e8246a8237e041b9bf89e6c56098c99
